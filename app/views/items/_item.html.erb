<% @items.each do |item| %>
    <li>
        <%= link_to "#{item.item_name} : #{item.price}   円", "items/#{item.id}" %>
        <%= link_to " 削除 ", item_path(item.id),id: "itemdeleteBtn", method: :delete %>
    </li>
<% end %>